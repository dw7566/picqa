# picqa — Photonic IC Quality Analyzer

A Python library for analyzing wafer-level test data from silicon photonic
process runs. Designed around the HY202103 dataset format (LION1 maskset,
1310 nm O-band devices).

## Status

Week 1 — foundations: package skeleton, CI, IO layer.

## Install

```bash
pip install -e ".[dev]"
```
